# Base URL of the Chabhuoy Laravel API.
# In dev, leave as "/api" to use the Vite proxy (vite.config.ts) and avoid CORS.
# In production, set the full origin, e.g. https://api.chabhuoy.com/api
VITE_API_URL=/api
